# conjugate gradient

# only works on quadratic functions but
# is often faster than solving with inverse at larger scale

# find conjugate directions:
# u_i is conjugate to u_j if u_i^T A u_j = 0

# for quadratics, the conjugate gradient converges in n steps
# this directly avoids the zig-zagging done by gradient descent

